import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: { flex: 1, padding: 10 },
    searchBar: { height: 40, borderColor: 'gray', borderWidth: 1, marginBottom: 10, borderRadius: 5, paddingHorizontal: 8 },
    card: { marginBottom: 10, padding: 10, borderWidth: 1, borderColor: '#ccc', borderRadius: 10 },
    imagePlaceholder: { fontStyle: 'italic', color: '#aaa' },
});

export default styles;